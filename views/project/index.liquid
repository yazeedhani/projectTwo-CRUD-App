{% layout 'layout.liquid'%}
{% block content %}
<div>
    <h2 class="display-6">All My Projects</h2>
    <div class="row">
        {% for project in projects %}
        <div class="card" style="width: 18rem;">
            <h5 class="card-header">{{ project.name }}</h5>
            <div class="card-body">
                <h5 class="card-title">
                    Owner: {{ project.owner }}
                </h5>
                <!-- <p class="card-text">
                    Body: {{ example.body }}
                </p>
                <p class="card-text">
                    Amount: {{ example.amount }}
                </p>
                <a href="/examples/{{ example.id }}" class="btn btn-info">
                    View {{ example.title }}
                </a> -->
            </div>
        </div>
        {% endfor %}
    </div>
</div>
{% endblock %}